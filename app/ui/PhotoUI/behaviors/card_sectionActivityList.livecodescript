script "card_sectionActivityList Behavior"
on preOpenCard
   # Following commands can be run when a UI change is required. They don't have to be run every time card is opened.
   #   setSize "desc", "field"
   #   set the textalign of field "desc" to left 
   # setsize "goTo", "button"
end preOpenCard
